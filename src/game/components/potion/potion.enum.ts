import { CardTargetedEnum } from '../card/card.enum';

export enum PotionRarityEnum {
    Common = 'common',
    Uncommon = 'uncommon',
    Rare = 'rare',
}

export const PotionTargetEnum = {
    ...CardTargetedEnum,
};
