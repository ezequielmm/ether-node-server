import { Gear } from '../gear.schema'
import { GearRarityEnum } from '../gear.enum'

export const Weapon_Yellow_Energy_Blade: Gear = {
    gearId: 17,
    name: 'Yellow_Energy_Blade',
    rarity: GearRarityEnum.Legendary,
};
