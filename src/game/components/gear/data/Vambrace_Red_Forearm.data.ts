import { Gear } from '../gear.schema'
import { GearRarityEnum } from '../gear.enum'

export const Vambrace_Red_Forearm: Gear = {
    gearId: 72,
    name: 'Red_Forearm',
    rarity: GearRarityEnum.Rare,
};
