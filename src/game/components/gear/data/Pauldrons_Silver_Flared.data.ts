import { Gear } from '../gear.schema'
import { GearRarityEnum } from '../gear.enum'

export const Pauldrons_Silver_Flared: Gear = {
    gearId: 154,
    name: 'Silver_Flared',
    rarity: GearRarityEnum.Common,
};
