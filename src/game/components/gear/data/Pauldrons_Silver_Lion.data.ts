import { Gear } from '../gear.schema'
import { GearRarityEnum } from '../gear.enum'

export const Pauldrons_Silver_Lion: Gear = {
    gearId: 54,
    name: 'Silver_Lion',
    rarity: GearRarityEnum.Epic,
};
