import { Gear } from '../gear.schema'
import { GearRarityEnum } from '../gear.enum'

export const Vambrace_Darkened: Gear = {
    gearId: 108,
    name: 'Darkened',
    rarity: GearRarityEnum.Uncommon,
};
