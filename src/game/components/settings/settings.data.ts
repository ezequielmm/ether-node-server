import { Settings } from './settings.schema';

export const settingsData: Settings = {
    player: {
        energy: {
            initial: 3,
            max: 3,
        },
        handSize: 5,
        deckSize: 10,
    },
};
