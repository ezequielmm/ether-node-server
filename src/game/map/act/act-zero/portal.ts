import { NodeType } from 'src/game/components/expedition/node-type';
import { NodeConfig } from '../act.builder';

export const portalNode: NodeConfig = {
    type: NodeType.Portal,
    subType: NodeType.Portal,
    data: {},
};
